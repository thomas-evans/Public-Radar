(function(){
    'use strict';
    ngDescribe({
        name: 'Test Directive',
        modules: ['directory', 'foo'],
        tests: function (deps) {
            it('can update DOM using binding', function () {

            });
        }
    });
}());
